sudo: required
services:
  - docker

before_install: 
  - docker build -t akash1412/docker-react-learning -f dockerfile.dev .


script:
  - docker run -e CI=true akash1412/docker-react-learning npm run test -- --coverage